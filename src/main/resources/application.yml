server:
  port: 80


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_ADDRESS:sh-cynosdbmysql-grp-n9fkk0fw.sql.tencentcdb.com:26105}/${MYSQL_DATABASE:springboot_demo}
    username: ${MYSQL_USERNAME:root}
    password: ${MySQL_PASSWORD:yqlzmzr1314@}
  mvc:
    view:
      suffix: .html
      prefix: /

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml
